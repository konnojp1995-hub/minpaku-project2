# Streamlit Community Cloud 再デプロイ方法

## 🔄 再デプロイの方法

### 方法1: GitHubにプッシュして自動再デプロイ（推奨・最も簡単）

Streamlit Community Cloudは、GitHubにプッシュすると自動的に再デプロイします。

```powershell
# 変更をコミット
git add .
git commit -m "Update files"
git push origin main
```

数分待つと自動的に再デプロイが開始されます。

### 方法2: アプリのメニューから再デプロイ

1. Streamlit Community Cloudのダッシュボードでアプリを開く
2. アプリ名をクリックしてアプリ詳細画面を開く
3. 右上の「**⋮**」（3つのドット）メニューをクリック
4. メニューから以下を探す：
   - 「**Redeploy**」
   - 「**Redeploy app**」
   - 「**Manage app**」→「**Redeploy**」
   - または「**⚙️ Settings**」→「**Redeploy**」

### 方法3: アプリを削除して再作成

もし上記の方法が使えない場合：

1. アプリの「⋮」メニューから「**Delete app**」を選択
2. 確認して削除
3. 「**New app**」から再度作成

## 🔍 Redeployボタンが見つからない場合

### UIが変更されている可能性

Streamlit Community CloudのUIは更新されることがあります。以下の場所を確認してください：

1. **アプリの詳細画面**
   - ダッシュボードでアプリをクリック
   - 詳細画面の右上や上部にボタンがある可能性

2. **Settings内**
   - 「⋮」→「Settings」
   - Settings画面内に「Redeploy」や「Deploy」ボタンがある可能性

3. **アプリ一覧画面**
   - アプリ一覧で、各アプリの横にボタンがある可能性

## ✅ 最も確実な方法：GitHubにプッシュ

`packages.txt`を修正したので、既にプッシュ済みです。

1. Streamlit Community Cloudのダッシュボードでアプリの状態を確認
2. 数分待つ（自動的に再デプロイが始まる場合があります）
3. アプリのログを確認（「⋮」→「View logs」または「View app logs」）

## 🔍 アプリの状態確認

アプリのダッシュボードで以下を確認：

- **Status**: 「Running」「Deploying」「Error」など
- **Last deployed**: 最終デプロイ時刻
- **Logs**: デプロイログを確認

ログを確認すると、再デプロイが進行中か、エラーが発生しているかが分かります。

## 📝 確認手順

1. Streamlit Community Cloudダッシュボードにアクセス
2. アプリ名をクリックして詳細を開く
3. 「**View logs**」または「**Logs**」タブを確認
4. 最新のログでデプロイ状況を確認

もしまだデプロイが開始されていない場合は、手動で何か小さな変更をコミット・プッシュすることで、強制的に再デプロイをトリガーできます：

```powershell
# 何か小さな変更（例：READMEの更新）
echo " " >> README.md
git add README.md
git commit -m "Trigger redeploy"
git push origin main
```
