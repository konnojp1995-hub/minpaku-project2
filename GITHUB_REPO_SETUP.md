# GitHubリポジトリ作成とプッシュ手順

「Repository not found」エラーが出る場合の解決方法です。

## 🔍 問題の原因

GitHubリポジトリがまだ作成されていないか、リポジトリ名が間違っています。

## ⚠️ 重要: .gitignoreの確認

プッシュ前に、`.env`ファイルがコミットされないように`.gitignore`を確認してください。

プロジェクトルートに`.gitignore`ファイルがあることを確認し、`.env`が含まれていることを確認してください。

## ✅ 解決手順

### ステップ1: GitHubでリポジトリを作成

1. **GitHubにログイン**
   - https://github.com にアクセス
   - ログイン（ユーザー名: `konno1995-hub`）

2. **新しいリポジトリを作成**
   - 右上の「**+**」アイコンをクリック
   - 「**New repository**」を選択

3. **リポジトリ情報を入力**
   - **Repository name**: `minpaku-chatbot` （または任意の名前）
   - **Description**: （任意）例: "民泊開業検討ツール"
   - **Public** または **Private** を選択
   - ⚠️ **重要**: 「Add a README file」「Add .gitignore」「Choose a license」は**チェックしない**（既にコードがあるため）

4. 「**Create repository**」をクリック

### ステップ2: ローカルのリモート設定を確認・修正

プロジェクトフォルダで以下を実行：

```powershell
# 現在のリモート設定を確認
git remote -v

# 間違ったリモートが設定されている場合、削除
git remote remove origin

# 正しいリモートを追加（リポジトリ作成後）
git remote add origin https://github.com/konno1995-hub/minpaku-chatbot.git

# 確認
git remote -v
```

### ステップ3: コードをプッシュ

```powershell
# 現在のブランチを確認
git branch

# mainブランチにいることを確認（いなければ作成・切り替え）
git branch -M main

# すべてのファイルをステージング
git add .

# コミット（まだコミットしていない場合）
git commit -m "Initial commit: Minpaku chatbot app"

# GitHubにプッシュ
git push -u origin main
```

## 🔧 トラブルシューティング

### エラー: "Authentication failed"

**解決方法:**
1. GitHubでPersonal Access Tokenを作成
   - 詳細な手順は [`GITHUB_TOKEN_GUIDE.md`](./GITHUB_TOKEN_GUIDE.md) を参照してください
   - 簡単な手順:
     1. GitHubにログイン → 右上のプロフィールアイコン → Settings
     2. 左メニュー最下部の「Developer settings」をクリック
     3. 「Personal access tokens」→「Tokens (classic)」をクリック
     4. 「Generate new token (classic)」をクリック
     5. Noteに用途を入力（例: `Streamlitデプロイ用`）
     6. Expirationで有効期限を選択
     7. **「repo」スコープにチェック**（重要！）
     8. 「Generate token」をクリック
     9. 表示されたトークンを**すぐにコピー**して保存
2. プッシュ時にパスワードの代わりにトークンを入力

### エラー: "Repository not found"

**確認事項:**
1. リポジトリが実際に作成されているか確認
   - https://github.com/konno1995-hub/minpaku-chatbot にアクセス
   - 404エラーが出る場合、リポジトリが存在しません
2. リポジトリ名が正しいか確認
3. ユーザー名が正しいか確認

### エラー: "Permission denied"

**解決方法:**
1. GitHubにログインしているか確認
2. リポジトリの所有者であることを確認
3. Personal Access Tokenの権限を確認（`repo`スコープが必要）

## 📝 簡単な確認コマンド

以下を順番に実行して、状態を確認：

```powershell
# 1. 現在のブランチを確認
git branch

# 2. リモート設定を確認
git remote -v

# 3. コミット履歴を確認
git log --oneline

# 4. ステータスを確認
git status
```

## ✅ 成功した場合

プッシュが成功すると、以下のようなメッセージが表示されます：

```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
Delta compression using up to X threads
Compressing objects: 100% (XX/XX), done.
Writing objects: 100% (XX/XX), XXX.XX KiB | XXX.XX KiB/s, done.
Total XX (delta X), reused 0 (delta 0), pack-reused 0 (from 0)
To https://github.com/konno1995-hub/minpaku-chatbot.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

その後、GitHubリポジトリのページでコードが確認できます！
